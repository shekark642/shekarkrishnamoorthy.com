// test-v4-script.js - a separate script file so it shows up as its own
// Resource Timing entry (initiatorType: "script") for HW3 Module 05.
// Deliberately trivial - only exists to be a loadable resource.
console.log('[test-v4-script] loaded');
